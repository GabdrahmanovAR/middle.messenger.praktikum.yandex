<div class="container">
  <div class="card register-card">
    <div class="form-title">Регистрация</div>

    <form action="" class="form-container register-card__form">
      <div class="field-group">
        {{> Field label="Почта" type="input" }}
        {{> Field label="Логин" type="input" }}
        {{> Field label="Имя" type="input" }}
        {{> Field label="Фамилия" type="input" }}
        {{> Field label="Телефон" type="input" }}
        {{> Field label="Пароль" type="password" }}
        {{> Field label="Пароль (еще раз)" type="password" }}
      </div>
      <div class="buttons-group">
        {{> Button label="Зарегистрироваться" type="primary" }}
        {{> Button label="Войти" type="link" }}
      </div>
    </form>
  </div>
</div>